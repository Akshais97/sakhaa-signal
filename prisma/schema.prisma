// Root Prisma schema pointer for default CLI discovery
// Canonical schema lives in packages/db/prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
  output   = "../packages/db/generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
